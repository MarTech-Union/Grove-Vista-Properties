import ContactSection from "@/components/ContactSection";

export const metadata = {
  title: "Contact",
  description: "Contact Grove Vista Properties for buyer guidance, investment advisory, and premium property support across India.",
  alternates: {
    canonical: "/contact",
  },
};

export default function ContactPage() {
  return <ContactSection />;
}
